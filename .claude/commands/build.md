Use the builder subagent to implement the following:

$ARGUMENTS

Read all relevant files first. Output complete, working code scoped only to what was asked. Leave the project in a buildable state when done.
