Use the architect subagent to produce a full system design and phase plan for the following:

$ARGUMENTS

Read the existing codebase context first, then produce the full architecture output as defined in the architect agent's instructions.
